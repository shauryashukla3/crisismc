kind = "web"
previewPath = "/"
title = "CrisisMC"
version = "1.0.0"
id = "artifacts/crisismc"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22454

[services.development]
run = "pnpm --filter @workspace/crisismc run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/crisismc", "run", "build" ]
publicDir = "artifacts/crisismc/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22454"
BASE_PATH = "/"
