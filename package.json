{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
   "preinstall": "echo using pnpm",
   "build": "corepack enable && corepack prepare pnpm@10.11.1 --activate && pnpm install --no-frozen-lockfile && pnpm --filter crisismc build",
   "deploy": "cd artifacts/crisismc && pnpm dlx wrangler deploy"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
